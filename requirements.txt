# Streamlit Community Cloud reads this file. streamlit_app.py shows the committed
# reports under negotiations/*.md and examples/*.md (JSON front matter) AND drives
# the real engine on user input: the schema validator (replay.schema.parse_report_text)
# and the cross-application matcher (replay.matcher.match_cross_applications).
# The `.` line installs this repo as a package so `replay` is importable on the Cloud.
streamlit>=1.40
.
